app = 'battle-city'
primary_region = 'yyz'

[build]

[[services]]
  internal_port = 3536
  protocol = "tcp"
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]

  [[services.ports]]
    port = 80
    handlers = ["http"]

[[vm]]
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256
